%h1 Listing surveys

%table.table.table-striped
  %tr
    %th Title
    %th Description
    %th Actions

  - @surveys.each do |survey|
    %tr
      %td= survey.title_link
      %td= survey.truncated_description
      %td
        = survey.edit_link
        = survey.destroy_link

%br

= link_to 'New Survey', new_survey_path, class: %w(btn btn-primary)
