%h1 Listing surveys

%table.table.table-striped
  %tr
    %th Title
    %th Description
    %th Actions

  - @surveys.each do |survey|
    %tr
      %td= survey.title_link
      %td= survey.truncated_description
      %td
        = link_to 'Edit', edit_survey_path(survey), class: %w(btn btn-mini)
        = link_to 'Destroy', survey, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: %w(btn btn-mini btn-danger)

%br

= link_to 'New Survey', new_survey_path, class: %w(btn btn-primary)
