%h1 New Question

= simple_form_for([@survey, @question]) do |f|
  .form-inputs
    = f.input :description
    = f.select :humanized_question_type, Question::TYPES
    = f.hidden_field :survey_id

  .form-actions
    = f.button :submit

= link_to 'Back', surveys_path